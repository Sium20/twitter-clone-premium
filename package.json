{
  "name": "twitter-clone-fullstack",
  "version": "1.0.0",
  "description": "Twitter clone with Node.js backend and Firebase frontend",
  "scripts": {
    "start:backend": "cd backend && npm start",
    "serve:frontend": "firebase serve --only hosting",
    "deploy:frontend": "firebase deploy --only hosting",
    "deploy:backend": "cd backend && railway deploy",
    "deploy:all": "npm run deploy:frontend && npm run deploy:backend",
    "install:backend": "cd backend && npm install",
    "setup:firebase": "firebase login && firebase init hosting",
    "setup:railway": "npm install -g @railway/cli && railway login",
    "setup:all": "npm run setup:firebase && npm run setup:railway"
  }
}
